'use strict';

const React = require('react');

class Library extends React.Component {
  constructor (props) {
    super(props);
  }

  toHTML () {
    
  }

  render () {
    return (
      <div>
        <h1>Library</h1>
      </div>
    );
  }
}

module.exports = Library;
